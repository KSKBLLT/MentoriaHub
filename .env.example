# Mentoria Hub — env (demo runs fully on in-memory store; these are optional/future)
# If you later wire Supabase, set these and flip the store implementation.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
